server:
  port: 8080

spring:
  application:
    name: ui
  profiles: 
    active: dev   
  thymeleaf: 
    encoding: UTF-8
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML5
    cache: false
      
#eureka:
#  client:
#    service-url:
#      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/ 
      